---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: buildjobs.topi.io
spec:
  group: topi.io
  names:
    kind: BuildJob
    listKind: BuildJobList
    plural: buildjobs
    singular: buildjob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: BuildJob is the Schema for the buildjobs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of BuildJob
            properties:
              artefactName:
                type: string
              owner:
                type: string
              ref:
                type: string
              repository:
                type: string
              suspend:
                type: boolean
            required:
            - artefactName
            - owner
            - ref
            - repository
            type: object
          status:
            description: status defines the observed state of BuildJob
            properties:
              buildEnd:
                description: BuildEnd is when the build ended
                format: date-time
                type: string
              buildStart:
                description: BuildStart is when the build started
                format: date-time
                type: string
              buildid:
                description: BuildID is the unique identifier for this build
                type: string
              commitsha:
                description: The commit sha, used to find the required config map
                type: string
              duration:
                description: BuildDuration is how long the build took
                type: string
              message:
                description: Message provides additional context about the current
                  phase
                type: string
              phase:
                description: Phase represents the current phase of the build job
                enum:
                - Pending
                - Queued
                - Running
                - Succeeded
                - Failed
                - Cancelled
                type: string
              requiredProviders:
                description: RequiredProviders lists the providers needed by the workflow
                items:
                  type: string
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
