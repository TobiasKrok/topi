apiVersion: v1
kind: ConfigMap
metadata:
  name: buildkit-config
  namespace: topi-system
data:
  buildkitd.toml: |
    # Allow insecure (HTTP) registry access
    [registry."registry.topi-system.svc.cluster.local:5000"]
      http = true
      insecure = true
